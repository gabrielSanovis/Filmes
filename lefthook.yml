pre-commit:
  jobs:
    - name: check-code
      group:
        parallel: true
        jobs:
          - name: lint
            run: yarn lint
          - name: format
            run: yarn format
    - run: git update-index --again